version: '2'

services:
    seafile:
        image: "sholl/seafile:dev"
        container_name: dc-seafile-ssl-server-dev
        ports:
         - "0.0.0.0:8443:8443"
        restart: always
        volumes:
         - "/opt/seafile:/opt/seafile"
         - "/etc/letsencrypt:/etc/letsencrypt"
